/* eslint-disable @typescript-eslint/no-unused-vars */

export const getUpcomingMaintenance = (userId: string) => {
  // TODO: Read scheduled service types
  // TODO: For each scheduled service type, if has vehiclesScheduled then read most recent scheduled log for vehicle and calculate its upcoming maintenance
  return;
};

export const getVehicleUpcomingMaintenance = (
  userId: string,
  vehicleId: string,
) => {
  // TODO: Read scheduled service types associated with vehicle
  // TODO: For each scheduled service type, if vehicleId in vehiclesScheduled then read most recent scheduled log for vehicle and calculate its upcoming maintenance
};
